import Foundation

// Stub — implementado na Fase 5.
//
// Aqui ficará a classe `SupabaseConnector` que adota
// `PowerSyncBackendConnectorProtocol` do PowerSync, com:
// - `fetchCredentials()` retornando JWT do Supabase Auth
// - `uploadData(database:)` aplicando a upload queue local no Postgres
//
// Enquanto não chamamos `db.connect(connector:)` em `AppContainer`, o app
// roda 100% local. A presença deste arquivo é só pra deixar a Fase 5 com
// caminho claro.
